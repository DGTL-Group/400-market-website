# Vendor-mapping conflicts & follow-ups

Source: `Vendors 2026-03-27 (2).xlsx` · extracted 136 vendors covering 321 booths.

Filter skipped: 10 rows (concessions / outside / parking lot / Building B).

Cross-reference rows skipped (Q1): 5.


<details><summary>Cross-ref rows</summary>

- See Georgian Bay (2501,2,3) → `1801, 1802(Booth & Half)`

- See Zio's (1713) → `2531`

- See Eze Dockers(2536) → `2538`

- See Eze Dockers(2536) → `3201`

- See Eze Dockers(2536) → `3503, 3504, 3505, 3506`

</details>


## Conflicts to resolve in the CRM

- **Fragrances Store (2402521 Ont)** — booth(s) `209B, 210`: Excel booths ['209B'] are not present in the SVG (the SVG has them as single booths without the A/B split). Mapping the remaining ['210'] for now; skip the flagged ones until the SVG is split.

- **Creative Expressions(Romayne)** — booth(s) `208, 209A`: Excel booths ['209A'] are not present in the SVG (the SVG has them as single booths without the A/B split). Mapping the remaining ['208'] for now; skip the flagged ones until the SVG is split.

- **Scottish Products (Jackies)** — booth(s) `1724A, 1724B, 1725A`: Excel booths ['1724A', '1724B', '1725A'] are not present in the SVG (the SVG has them as single booths without the A/B split). Mapping the remaining [] for now; skip the flagged ones until the SVG is split.

- **Zio's Part 2(Pomodoro)** — booth(s) `1725B`: Excel booths ['1725B'] are not present in the SVG (the SVG has them as single booths without the A/B split). Mapping the remaining [] for now; skip the flagged ones until the SVG is split.

- **Susan Wade (JW)** — booth(s) `2522, 2523A`: Excel booths ['2523A'] are not present in the SVG (the SVG has them as single booths without the A/B split). Mapping the remaining ['2522'] for now; skip the flagged ones until the SVG is split.
